# Frontend API base URL. The backend listens on port 3001 by default.
VITE_BACKEND_URL=http://localhost:3001

# Frontend WebSocket base URL. Keep the trailing slash for the current WebSocket path.
VITE_WEBSOCKET_URL=ws://localhost:3001/

# Local backend port: 3001
